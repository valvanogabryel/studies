export default class SentryObjects {
    constructor(id, name, minYear, maxYear) {
        this.id = id;
        this.name = name;
        this.minYear = minYear;
        this.maxYear = maxYear;
    }
}